/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.up = function(knex) {
  return knex.raw(`INSERT INTO "Detailed-catalog" ("id_name", "description_1")
  SELECT id, name FROM catalog WHERE name IS NOT NULL
  `)
};

/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.down = function(knex) {
  return knex('Detailed-catalog').del()
};
